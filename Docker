#baixa a img da distribuição do hub docker
docker pull <distro> 

#Cria o container
docker run -d -t --name <NOME_DO_CONTAINER> <distro> 

#entra no bash do container criado
docker exec -it <NOME_DO_CONTAINER>  bash 

#mostra todos os containers criados
docker ps 
